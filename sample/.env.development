# Whether to open mock
VITE_USE_MOCK = true

# public path
VITE_PUBLIC_PATH = /

# api url prefix (mock 模式下使用 /mock，与生产 /api 区分)
VITE_GLOB_API_URL = /mock

# 账号相关接口走本地服务，其余接口继续使用 mock
VITE_ACCOUNT_API_URL = /api
VITE_USE_PROXY = true
VITE_PROXY = [["/api", "http://127.0.0.1:9000"]]
